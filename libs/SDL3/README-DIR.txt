To build on Windows with Visual Studio:
Download SDL3 from https://github.com/libsdl-org/SDL/releases (the devel VC
version) and go down a level when you extract so the top level directories here
should be docs\ include\ libs\ etc rather than the SDL3-3.n-nn directory.