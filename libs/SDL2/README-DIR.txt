To build on Windows with Visual Studio:
Download SDL2 from http://libsdl.org/download-2.0.php (the Visual C++ development
version) and go down a level when you extract so the top level directories here
should be docs\ include\ libs\ etc rather than the SDL2-2.n-nn directory.